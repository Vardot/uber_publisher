/**
 * @file
 * Behaviors for the uber_publisher_bs4 theme.
 */

(function ($, _, Drupal, drupalSettings) {
  'use strict';

  Drupal.behaviors.uber_publisher_bs4 = {
    attach: function (context) {

      // uber_publisher_bs4 JavaScript behaviors goes here.
    }
  };

})(window.jQuery, window._, window.Drupal, window.drupalSettings);
